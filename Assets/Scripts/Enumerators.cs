public class Enumerators {

	// Note: n = Note assigned

	public enum playerForm { small, big, flower, na };
	public enum playerState { normal, invincible, star, dead, na };

	public enum PowerUpType { mushroom, flower, star, na };
	public enum EnemyType { goomba, turtle, flower, fireball, na };

}
